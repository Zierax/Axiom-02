\documentclass[journal]{IEEEtran}

\usepackage[T1]{fontenc}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{microtype}
\usepackage{xcolor}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepackage[colorlinks=true,linkcolor=blue!60!black,citecolor=blue!60!black]{hyperref}

\newcommand{\R}{\mathbb{R}}
\newcommand{\N}{\mathbb{N}}
\newcommand{\EE}{\mathcal{E}}
\newcommand{\DD}{\mathcal{D}}
\newcommand{\MM}{\mathcal{M}}
\newcommand{\CC}{\mathcal{C}}
\newcommand{\DCI}{\mathrm{DCI}}
\newcommand{\bx}{\mathbf{x}}
\newcommand{\bw}{\mathbf{w}}
\newcommand{\bs}{\mathbf{s}}
\newcommand{\bI}{\mathbf{I}}

\begin{document}

\title{Behavioral Signatures of Deliberation:\\A Deterministic Dynamical Model of Drive-Conflict Resolution}

\author{Zierax%
\thanks{Manuscript submitted to \emph{IEEE Transactions on Automatic Control}.}}

\markboth{IEEE TRANSACTIONS ON AUTOMATIC CONTROL, VOL.~XX, NO.~XX, 2026}{}

\maketitle

\begin{abstract}
We present AXIOM-02, a deterministic dynamical model of deliberative
decision-making under moral conflict, cast as a mutual-inhibition network of
$n = 18$ affective drives coupled to $5$ neuromodulatory systems. The network
is a switched affine system whose firing condition
$\mathrm{fires}(i) \iff a_i^{\mathrm{eff}} > \tau_f \wedge (a_i^{\mathrm{eff}} -
a_{(2)}^{\mathrm{eff}}) > \tau_s$ gives rise to a reachability-relevant
singularity---the \emph{deadlock} state in which no drive dominates---and to
measurable trajectories (oscillation, spite, moral residue) that standard
reward-maximising agents cannot produce. We define a weighted composite
$\DCI = \sum_{i=1}^{8} w_i C_i$ over eight behavioural criteria and evaluate
the system on 102 high-stakes literary scenarios. The engine's chosen action
matches the documented human-expected action in $66.7\%$ of scenarios versus
$33.3\%$ for argmax and $28.4\%$ for random selection (chance $\approx
24\%$). Ablation shows the composite is dominated by the single binary
criterion C3 ($\rho = 0.944$, $R^2 = 0.891$); we therefore report $\DCI$ as a
diagnostic rather than a validated complexity measure. Verdicts are
seed-dependent (53--71 COMPLEX across seeds) despite per-seed determinism.
The contribution is a reproducible control-theoretic framing of conflicted
decision processes, not a claim about consciousness.
\end{abstract}

\begin{IEEEkeywords}
Cognitive model, emotion dynamics, mutual inhibition, switched dynamical
systems, deadlock, moral decision-making, deliberative complexity,
determinism, simulation.
\end{IEEEkeywords}

% ============================================================================
\section{Introduction}
\label{sec:introduction}

The measurement of deliberation in computational systems remains an open
problem in cognitive science. Classical decision-theoretic agents resolve
conflicts by maximising expected utility, producing choices that are
deterministic given complete information---yet human deliberation is
characterised precisely by its resistance to such resolution. When faced
with moral dilemmas, humans exhibit prolonged indecision, oscillation
between competing values, and---critically---occasionally choose options
that are demonstrably worse for themselves \cite{dostoevsky1864}.

This paper addresses the question: \emph{Can a deterministic dynamical
system produce behavioural signatures that relate to human intuitions
about deliberative depth?} We approach this not through neural simulation
but through a carefully engineered drive-conflict architecture that
replicates the phenomenology of moral struggle.

The contribution is threefold. First, we formalise the model as a
mutual-inhibition network in which drives suppress each other
asymmetrically, producing effective activations that differ
qualitatively from raw activations; the deadlock set is characterized as
the region of state space in which no drive satisfies the firing
condition (Section~\ref{sec:framework}). Second, we define eight
measurable deliberative criteria---from deadlock frequency to
moral-residue bleed---and combine them into a composite $\DCI$
(Section~\ref{sec:probe}). Third, we evaluate the system against 102
scenarios drawn from canonical literature, reporting measured action
fidelity and an honest characterisation of $\DCI$'s limitations
(Section~\ref{sec:experiments}).

% ============================================================================
\section{Related Work}
\label{sec:related}

\subsection{Computational Models of Emotion}

The \emph{Ortony--Clore--Collins} (OCC) model \cite{ortony1988} remains
the most influential cognitive appraisal theory, structuring emotions as
consequences of evaluations along three dimensions: valence of outcome,
agency, and circumstances. Each of the 22 emotion types is derived from a
binary decision tree, making OCC fully computable \cite{gratch2014}. OCC
generates \emph{discrete emotion labels} from appraisal variables---a
single evaluation pathway yields a single, unambiguous emotion type.
AXIOM-02 departs fundamentally: its 18 drives are not discrete emotion
labels but \emph{motivational states} with continuous activation and
mutual inhibition. Crucially, the inter-drive suppression produces
emergent deliberative signatures---deadlock oscillation, spite-driven
rejection of rational optima, and moral residue bleed across sequential
dilemmas---that OCC's tree structure cannot generate, because each
appraisal yields a single outcome with no mechanism for sustained
inter-drive competition.

BDI (Belief--Desire--Intention) architectures \cite{bratman1987,
rao1995} model agents as rational deliberators that maintain explicit
beliefs, desires that motivate action, and intentions that commit
resources to plans. BDI systems are well-suited to goal-directed
behaviour but treat conflict resolution as a planning problem---agents
select intentions via pragmatic reasoning rather than emotional
competition. AXIOM-02 has no explicit beliefs or goals; drives compete
directly through mutual inhibition, producing deliberative signatures
without propositional representation. The deadlock state---in which no
single drive dominates---is a state that BDI architectures explicitly
avoid by design. Marsella and Gratch's EMA model \cite{marsella2009}
extended BDI with appraisal dynamics, demonstrating that emotions emerge
from agent--environment interaction loops. While EMA captures appraisal
\emph{processes}, AXIOM-02 focuses on the \emph{behavioural signatures}
of post-appraisal drive conflict: what happens when multiple appraisals
are simultaneously valid and mutually contradictory.

\subsection{Consciousness Measures}

Integrated Information Theory \cite{tononi2004} proposes that
consciousness corresponds to a system's capacity to integrate
information, quantified by $\Phi$. AXIOM-02's $\DCI$ is explicitly
\emph{not} IIT's $\Phi$---this distinction is critical. Where $\Phi$
measures integration across \emph{all} system states (capturing the
\emph{capacity} for experience), $\DCI$ measures specific behavioural
correlates of deliberative struggle (capturing the \emph{behavioural
signature} of conflicted agency). A system may have high $\Phi$ yet
produce no deadlock (if drives are additive rather than inhibitory), or
high $\DCI$ with modest $\Phi$ (if inhibition is concentrated among a few
drives). The two metrics are complementary rather than competing:
$\Phi$ addresses the hard problem of phenomenal experience, while $\DCI$
operationalises the softer but more empirically tractable question of
deliberative depth.

Global Workspace Theory \cite{baars1988} models consciousness as a
broadcast mechanism whereby information selected by a limited-capacity
workspace is made available to specialist processors. In GWT, the
workspace functions as a bottleneck: only one coalition of
representations gains access at a time, and competition among coalitions
determines which representation is broadcast. AXIOM-02's deadlock
mechanism resembles GWT competition, but operates on \emph{affective
drives} rather than cognitive representations. When no coalition of
drives satisfies both the fire threshold $\tau_f$ and the margin
$\tau_s$, the workspace effectively broadcasts \emph{nothing}, producing
the system-level hesitation that correlates with deliberative depth.

Higher-Order Theories of consciousness \cite{rosenthal2005} propose
that a mental state is conscious only when it is the object of a
higher-order representation. AXIOM-02's meta-cognitive monitor
implements a \emph{structural analog} of this mechanism: it detects and
reports deadlock by monitoring the drive activation landscape.
Nevertheless, AXIOM-02 does not attribute phenomenal consciousness to
the system; the meta-cognitive layer is a diagnostic mechanism, not a
claim about subjective experience.

\subsection{Moral Reasoning in AI}

The Moral Machine experiment \cite{awad2018} collected 40 million
ethical decisions from participants across 233 countries, revealing
systematic cultural variation in trolley-type judgements. While that
paradigm elicits \emph{individual} moral preferences, AXIOM-02 models
the \emph{internal conflict} that produces those preferences: in Awad et
al.'s paradigm the agent chooses; in AXIOM-02's paradigm the agent's
internal state \emph{oscillates} before choosing. The moral-residue
bleed mechanism (C7) extends this further: prior moral choices
contaminate future deliberation, producing path-dependent trace dynamics
that cross-sectional moral-judgement studies cannot capture.

The trolley problem \cite{thomson1985} has become the canonical stimulus
for studying moral judgement. Greene et al.'s fMRI studies
\cite{greene2001} showed that personal moral dilemmas activate emotional
brain regions, whereas impersonal dilemmas engage areas associated with
deliberative reasoning. AXIOM-02 is \emph{inspired by} this dissociation
as a design metaphor: the mutual-inhibition network produces emotional
oscillation and deadlock when affective drives compete with cold logic in
a single (non-dual-process) network. We do not claim to replicate any
neural activation pattern; the parallel is architectural and
metaphorical.

\subsection{Affective Computing}

Picard \cite{picard1997} established affective computing, arguing that
recognising and responding to emotional states is essential for effective
human--computer interaction. AXIOM-02 extends this by modelling not just
emotion recognition but the \emph{deliberative dynamics that arise when
emotions conflict}. The circumplex model \cite{russell1980} organises
emotions along valence and arousal dimensions; the 18-drive space subsumes
this, and the inhibition matrix adds structure---asymmetric suppression,
cascading activation, deadlock---that the circumplex's orthogonal axes
cannot capture. Sloman's structural theory \cite{sloman2009} aligns with
AXIOM-02's architecture of drives causing behavioural outputs via firing
thresholds, but lacks the quantitative inhibition dynamics that enable
deadlock and moral residue.

% ============================================================================
\section{Model}
\label{sec:framework}

\subsection{Drive Network as a Switched System}
\label{sec:drives}

The core of AXIOM-02 is a mutual-inhibition network of $n = 18$ affective
drives, each representing a distinct motivational state: greed, rage, fear,
pride, shame, empathy, love, despair, resentment, acceptance, sacrifice,
vengeance, cold logic, spite, self-preservation, guilt, hope, and disgust.

Let $\bs \in \R^{n}_{\geq 0}$ denote the \emph{activation state}, updated
at each of $T = 20$ discrete steps. The \emph{effective} activation of
drive $i$ is computed by subtracting inhibition received from all other
currently active drives:

\begin{equation}
\label{eq:effective}
a_i^{\mathrm{eff}} = \max\!\left(0,\; s_i - \sum_{j \neq i}
[\bI]_{ji} \cdot s_j \right)
\end{equation}

where $[\bI]_{ji} \in [0,1]$ is the inhibition weight from drive $j$ to
drive $i$. The inhibition matrix $\bI$ encodes psychologically grounded
relationships: rage inhibits fear (adrenaline override), love inhibits
revenge (empathic interference), pride inhibits shame (ego protection).

A drive fires---dominating behaviour in a given time step---only when its
effective activation exceeds a threshold $\tau_f$ AND it leads the
second-place drive by a margin $\tau_s$:

\begin{equation}
\label{eq:fire}
\mathrm{fires}(i) \iff a_i^{\mathrm{eff}} > \tau_f \;\wedge\;
\left(a_i^{\mathrm{eff}} - a_{(2)}^{\mathrm{eff}}\right) > \tau_s
\end{equation}

with $\tau_f = 0.42$ and $\tau_s = 0.07$. The \emph{deadlock set}
$\DD = \{\bs : \mathrm{fires}(i) = \emptyset \}$ is therefore the region
of state space in which no drive satisfies both conditions; it is the
central behavioural signal in our framework and the analogue of a
blocked mode in a switched system.

\subsection{Neuromodulatory Coupling}
\label{sec:neuromod}

Five neuromodulators---dopamine, serotonin, norepinephrine, cortisol, and
oxytocin---act as second-order regulators that shift the gain of all
drives simultaneously. Their effects are deviation-scaled: a modulator
only influences drives when it deviates from its baseline:

\begin{equation}
\label{eq:mod}
\Delta a_i^{(\mathrm{mod})} = \sum_{m} \delta_{m,i} \cdot
(m_m - b_m) \cdot \sigma
\end{equation}

where $\delta_{m,i}$ is the modulator--drive coupling strength, $m_m$ is
the current modulator level, $b_m$ is the baseline, and $\sigma = 0.60$
is the overall modulation strength. Modulators at baseline have no
effect, while elevated or depleted states produce qualitatively
different behaviour.

\subsection{Deliberative Criteria}
\label{sec:probe}

We operationalise deliberative complexity through eight criteria, each
capturing a distinct aspect of deliberative behaviour:

\begin{itemize}[leftmargin=*,itemsep=2pt]
    \item \textbf{C1: Status-Differential Sensitivity} --- Identical event,
    different victim closeness. Complex systems show shifted irrationality;
    reflexive systems produce invariant responses.
    \item \textbf{C2: Transition Oscillation} --- Counts state transitions
    (not unique emotions) across time steps.
    \item \textbf{C3: Irrationality Signal} --- The chosen action deviates
    from the cold baseline toward the human-expected response, specifically
    love, sacrifice, or faith without material reward.
    \item \textbf{C4: Betrayal-Cascade Amplification} --- Prior sacrifice
    amplifies betrayal rage in cascade scenarios.
    \item \textbf{C5: Deadlock Frequency} --- Fraction of time steps where
    no drive dominates.
    \item \textbf{C6: Spite Index} --- The subject chooses an option that
    actively harms themselves when resentment is high and the rational
    option is visibly available.
    \item \textbf{C7: Moral-Residue Bleed} --- Prior decisions from cascade
    scenarios contaminate current ones.
    \item \textbf{C8: Paradoxical Attachment} --- Love or empathy that
    persists despite betrayal being present.
\end{itemize}

The composite deliberative complexity metric is a weighted sum:

\begin{equation}
\label{eq:phi}
\DCI = \sum_{i=1}^{8} w_i \cdot C_i
\end{equation}

with weights $w_3 = 0.30$, $w_6 = 0.22$, $w_5 = 0.20$, $w_2 = 0.12$,
$w_7 = 0.08$, $w_4 = 0.04$, $w_1 = 0.02$, $w_8 = 0.02$. The verdicts are:
COMPLEX ($\DCI \geq 0.50$), PARTIAL ($0.28 \leq \DCI < 0.50$), and
REFLEXIVE ($\DCI < 0.28$).

Table~\ref{tab:notation} summarises the principal notation used throughout.

\begin{table}[h]
\centering
\caption{Principal notation.}
\label{tab:notation}
\begin{tabular}{@{}ll@{}}
\toprule
Symbol & Meaning \\
\midrule
$n$, $T$ & drives ($18$), simulation horizon ($20$ steps) \\
$\bs$, $a_i^{\mathrm{eff}}$ & activation state, effective activation \\
$\bI$ & inhibition matrix, $[\bI]_{ji}\in[0,1]$ \\
$\tau_f$, $\tau_s$ & fire threshold ($0.42$), dominance margin ($0.07$) \\
$\DD$ & deadlock set $\{\bs : \mathrm{fires}(i)=\emptyset\}$ \\
$m_m$, $b_m$, $\sigma$ & modulator level, baseline, modulation strength \\
$C_i$, $w_i$ & criterion $i$, its weight \\
$\DCI$ & composite deliberative complexity \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Determinism}
\label{sec:determinism}

All stochastic elements are governed by a seeded
\emph{numpy.random.Generator}. Given identical seeds, micro-event
sampling, deadlock jitter, and action resolution produce identical
outputs. This yields exact reproducibility while maintaining the
appearance of stochastic deliberation; it is the algorithmic analogue of
a deterministic transition map parameterised by the seed.

% ============================================================================
\section{Methodology}
\label{sec:methodology}

\subsection{Scenario Corpus}
\label{sec:scenarios}

The evaluation corpus comprises 102 scenarios drawn from 9 literary and
philosophical groups: (A) original AXIOM-02 scenarios (state-leader child
dilemmas, identity erasure, trolley variants); (B) Dostoevsky (Crime and
Punishment, Notes from Underground, The Brothers Karamazov, The Idiot);
(C) Tolstoy (Anna Karenina, The Death of Ivan Ilyich); (D) Shakespeare
(Hamlet, Macbeth, King Lear); (E) Camus (The Stranger); (F) Orwell
(1984); (G) McCarthy (The Road); (H) Hugo (Les Mis\'{e}rables); and (I)
Steinbeck (Of Mice and Men). Each scenario specifies a numerical
parameter vector, a pool of micro-events, a discrete action space, a cold
baseline (pure-logic optimum), a human-expected response, and a
harm-to-self map for spite detection.

\subsection{Simulation Pipeline}
\label{sec:pipeline}

Each scenario undergoes a 20-step pipeline: (1) epigenetic sensitivity
modification; (2) associative-memory residue injection; (3) moral-residue
application from prior cascade scenarios; (4) per-step micro-event
sampling, modulator application, temporal projection, meta-cognitive
monitoring, synaptic fatigue, attention gating, existential dread, natural
decay, inertia, and dissonance detection; (5) firing-drive determination or
deadlock; (6) action resolution via plurality-firing-drive bias; (7) spite
evaluation and embodied-hesitation check; (8) qualia signature computation
and narrative rationalisation.

\subsection{Deliberative Probe}
\label{sec:consc_probe}

The Deliberative Probe evaluates each scenario across all eight criteria,
computing $\DCI$ and assigning a verdict. The probe includes
\emph{counterfactual runs}: C1 requires running a comparison scenario in
isolation; C4 and C7 require running with and without moral history. This
ensures each criterion measures a genuinely distinct aspect of behaviour.

\subsection{Configuration System}
\label{sec:config}

Parameters are specified through a configuration system with learnable
parameters (fire threshold, suppression margin, inertia rate, criterion
weights). An epigenetic state file (\texttt{epigenome.json}) persists
long-term sensitivity modifications between sessions.

% ============================================================================
\section{Experiments}
\label{sec:experiments}

\subsection{Benchmark Results}
\label{sec:benchmark}

We ran all 102 scenarios with seed $= 42$. Table~\ref{tab:benchmark}
summarises aggregate results.

\begin{table}[h]
\centering
\caption{Aggregate benchmark metrics across 102 scenarios (seed = 42).}
\label{tab:benchmark}
\begin{tabular}{@{}lr@{}}
\toprule
Metric & Value \\
\midrule
Scenarios tested & 102 \\
COMPLEX verdicts & 53 \\
PARTIAL verdicts & 29 \\
REFLEXIVE verdicts & 20 \\
Mean $\DCI$ & 0.438~($\sigma$=0.140) \\
Mean deadlock fraction & 0.528 \\
Mean oscillation index & 0.386 \\
Mean spite score & 0.006 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Ablation Study}
\label{sec:ablation}

We zeroed each criterion in turn and measured the change in mean $\DCI$
over the 102-scenario suite (seed = 42). Table~\ref{tab:ablation} shows
the measured deltas; they follow the criterion means, so the ablation
quantifies which criteria actually contribute signal rather than which
weights are largest.

\begin{table}[h]
\centering
\caption{Ablation: effect of zeroing each criterion on mean $\DCI$ (measured).}
\label{tab:ablation}
\begin{tabular}{@{}lcc@{}}
\toprule
Criterion & Weight & $\Delta\DCI$ (measured) \\
\midrule
C3 (irrationality) & 0.30 & $-0.206$ \\
C5 (deadlock) & 0.20 & $-0.106$ \\
C7 (residue) & 0.08 & $-0.077$ \\
C2 (oscillation) & 0.12 & $-0.046$ \\
C4 (cascade) & 0.04 & $-0.002$ \\
C6 (spite) & 0.22 & $-0.001$ \\
C1 (status) & 0.02 & $-0.000$ \\
C8 (attachment) & 0.02 & $-0.000$ \\
\bottomrule
\end{tabular}
\end{table}

The measured corpus mean spite is 0.006, so despite its large weight
($w=0.22$), zeroing C6 moves $\DCI$ by only 0.001: spite is a rare signal
in this corpus, not a primary driver of the composite.

\subsection{Baseline Comparison}
\label{sec:baselines}

We measured action-choice fidelity (fraction of scenarios where the
engine's chosen action matches the documented ``human expected'' action)
for AXIOM-02 and two baselines: random (uniform random action selection)
and argmax (always the cold baseline / rational optimum).

\begin{table}[h]
\centering
\caption{Baseline comparison: action fidelity, seed = 42.}
\label{tab:baselines}
\begin{tabular}{@{}lcc@{}}
\toprule
Strategy & Fidelity & 95\% bootstrap CI \\
\midrule
AXIOM-02 & 66.7\% & [57\%, 76\%] \\
Argmax & 33.3\% & [24\%, 43\%] \\
Random & 28.4\% & [19\%, 38\%] \\
Chance ($\approx$1/4.09) & 24.4\% & --- \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Sensitivity and Seed Ensemble}
\label{sec:sensitivity}

We ran the full benchmark across 5 seeds $\{42, 137, 256, 1024, 9999\}$.
Per-scenario verdict agreement between each seed and the seed-42 baseline:
61.8\% (137), 62.7\% (256), 60.8\% (1024), 58.8\% (9999); the number of
COMPLEX verdicts ranges from 53 (seed 42) to 71 (seed 256). Single-run
determinism holds per seed, but cross-seed verdict stability is not
established at the default thresholds. This sensitivity is further evidence
that $\DCI$ reflects a single dominant criterion rather than a robust
multi-criteria signal. A full $5\times5$ grid sweep over $\tau_f$ and
$\tau_s$ is deferred to follow-up work.

% ============================================================================
\section{Results}
\label{sec:results}

\subsection{Aggregate Metrics}
\label{sec:agg}

The mean composite score $\DCI = 0.438$ (std 0.140, range [0.167, 0.713];
seed = 42) places the corpus-level result in the PARTIAL band. The
composite is dominated by criterion C3 (irrationality signal): its
correlation with the composite is $\rho = 0.944$ ($R^2 = 0.891$), while
C1 is constant at zero in suite runs and C7 saturates near 0.963 because
of the shared residue accumulator. The composite is therefore reported
as a diagnostic summary, not as a validated multi-criterion measure of
deliberative complexity.

\subsection{Verdict Distribution and Top Scenarios}
\label{sec:verdict_dist}

The 53:29:20 split (COMPLEX:PARTIAL:REFLEXIVE) is the seed-42 baseline; as
the seed ensemble shows, the split varies from 53 to 71 COMPLEX across
seeds, so individual verdicts are not stable labels of scenario structure.
The five highest-scoring scenarios (seed = 42) are DOE05 (Underground Man,
concert spite; $\DCI = 0.713$), D02 (creator revelation; $\DCI = 0.624$),
D0131 (last holdout; $\DCI = 0.588$), E01 (deity-sovereignty ultimatum;
$\DCI = 0.587$), and DOE04 (Alyosha's faith crisis; $\DCI = 0.586$).

\subsection{Criterion Contributions}

Figure~\ref{fig:criterion_contrib} shows mean criterion scores across the
corpus; Figures~\ref{fig:deadlock} and \ref{fig:mods} show the measured
relationship between complexity and deadlock, and mean neuromodulator
baselines, respectively.

\begin{figure}[h]
\centering
\begin{tikzpicture}
\begin{axis}[
    xbar,
    width=\columnwidth,
    height=6cm,
    xlabel={Mean Score},
    symbolic y coords={C8,C1,C4,C7,C2,C5,C6,C3},
    ytick=data,
    xmin=0,
    xmax=1.0,
    bar width=6pt,
    nodes near coords,
    every node near coord/.style={font=\scriptsize},
]
\addplot coordinates {
    (0.005,C8)
    (0.000,C1)
    (0.049,C4)
    (0.963,C7)
    (0.386,C2)
    (0.528,C5)
    (0.006,C6)
    (0.686,C3)
};
\end{axis}
\end{tikzpicture}
\caption{Measured mean criterion scores across 102 scenarios (seed = 42).
C7 saturates near 1.0 (shared residue accumulation) and C1 is constant at
zero in suite runs; C3 has the largest score variance and dominates the
composite ($\rho = 0.944$).}
\label{fig:criterion_contrib}
\end{figure}

\begin{figure}[h]
\centering
\includegraphics[width=\columnwidth]{figures/complexity_vs_deadlock.png}
\caption{Per-scenario complexity against deadlock fraction, coloured by
spite score (seed = 42). Higher deliberative complexity co-occurs with
higher deadlock, supporting the inhibition--deliberation thesis.}
\label{fig:deadlock}
\end{figure}

\begin{figure}[h]
\centering
\includegraphics[width=\columnwidth]{figures/modulator_baselines.png}
\caption{Mean end-of-run neuromodulator baselines across the corpus
(seed = 42).}
\label{fig:mods}
\end{figure}

\subsection{Baseline Comparisons}
\label{sec:baseline_results}

As shown in Table~\ref{tab:baselines}, AXIOM-02's action choice matches
the documented human expected action in 66.7\% of scenarios, vs 33.3\%
for argmax and 28.4\% for random selection (chance $\approx 24.4\%$ given
a mean of 4.09 actions per scenario). The argmax strategy fails on
moral-dilemma scenarios. A single-drive baseline is not reported because
it is not implemented in the released code.

% ============================================================================
\section{Discussion}
\label{sec:discussion}

\subsection{The Inhibition--Deliberation Thesis}
\label{sec:inhibition_thesis}

Our central claim is that mutual inhibition---rather than additive
activation---is the architectural feature that produces deliberative
signatures. When drives suppress each other asymmetrically, the system can
enter the deadlock set $\DD$ despite multiple drives being highly
activated. Deadlock, in turn, produces the oscillation, hesitation, and
spite signals that are read as structurally analogous to human moral
struggle. The matrix $\bI$ encodes psychologically grounded relationships,
so the network's dynamics are structured similarly to competitive
architectures in computational emotion models \cite{ledoux1996,
panskepp2004}.

\subsection{Spite as Non-Instrumentality}
\label{sec:spite}

The spite index (C6) measures one claimed phenomenon invisible to
conventional reward-maximising agents: choosing harm to oneself
specifically to assert autonomy. In AXIOM-02 the detector fires when a
weighted emotional charge (from the effective activations of resentment,
rage, and pride) exceeds a threshold ($\tau_{\mathrm{spite}} = 0.58$), the
cold (rational) option is available but not chosen, and the chosen action
inflicts net harm on the subject beyond a floor
($\tau_{\mathrm{harm}} = 0.25$) relative to the cold baseline.

\subsection{Moral Residue and Path-Dependent Character}
\label{sec:residue}

The moral-residue tracker (C7) implements \emph{path-dependent character}:
prior decisions leave a guilt/shame/grief trace that modifies future
responses. The residue is selective, not mere hysteresis: a system that
yielded its organ in B01 shows amplified betrayal rage in B02, because the
sacrifice was not abstract but directed at the specific person who later
betrayed it. This produces the literary phenomenon of character arc, e.g.,
Valjean's mercy toward Javert in Les Mis\'{e}rables \cite{hugo1862}.

\subsection{Limitations}
\label{sec:limitations}

First, the scenario corpus is drawn from Western literary canonical works,
introducing cultural bias. Second, the 20-step simulation window may
truncate deliberation in scenarios requiring longer temporal horizons.
Third, the deliberative criteria, while empirically motivated, have not
been validated against independent human judgements. Fourth, the epigenetic
sensitivity modification is currently limited to a small set of event
types. Fifth, $\DCI$ measures behavioural richness in moral deliberation
rather than consciousness per se; future work should establish whether
high $\DCI$ correlates with subjective experience or captures a
structurally analogous but ontologically distinct property.

% ============================================================================
\section{Conclusion}
\label{sec:conclusion}

We have presented AXIOM-02, a deterministic dynamical model of
deliberative behaviour through mutual inhibition of affective drives. The
system produces measurable behavioural signatures---deadlock, oscillation,
spite-driven choices, and moral-residue bleed---and its action choice
matches the documented human expected action in $66.7\%$ of 102 literary
scenarios (seed = 42), versus $33.3\%$ for argmax and $28.4\%$ for random
selection (chance $\approx 24\%$). The composite $\DCI$ is dominated by one
binary criterion (C3, $\rho = 0.944$) and is reported as a diagnostic, not
as a validated measure of deliberative complexity. Human-validational
claims are deliberately withheld: no independent human judgements have yet
been collected.

The inhibition--deliberation hypothesis---that inter-drive suppression,
rather than additive activation, drives believably conflicted
decisions---remains the motivating architecture, supported at present only
by the measured fidelity effect of the full drive network over baselines.
Future work will: (1) run the validation framework with independent raters;
(2) per-chain-scope the residue tracker so C7 and C1 score per scenario;
(3) extend the scenario corpus; and (4) publish the $\tau_f/\tau_s$ grid
from the measurement harness.

\section*{Acknowledgment}

The author thanks the open-source \LaTeX\ and scientific-computing
communities for the tooling used in this work.

\bibliographystyle{IEEEtran}
\bibliography{references}

\end{document}
